# Test Report — v0.5.2 Discovery-Sync Integration

**Date:** 2026-04-01
**Status:** PASS

## Summary

| Suite | Tests | Passed | Failed |
|-------|-------|--------|--------|
| services/test_market_sync | 25 | 25 | 0 |
| Full backend suite | 189 | 189 | 0 |

(Prior: 183 tests. +6 new TestSyncDiscoveryIntegration tests added.)

## New Tests (TestSyncDiscoveryIntegration)

- `test_inactive_market_not_written` — active=False → discovery rejects → registry empty
- `test_closed_market_not_written` — closed=True → discovery rejects → registry empty
- `test_missing_dates_market_not_written` — source_timestamp=None → discovery rejects → registry empty
- `test_duration_out_of_range_not_written` — duration > 360s → discovery rejects → registry empty
- `test_only_valid_candidates_written_in_mixed_batch` — mixed batch: valid written, rejected silently dropped
- `test_discovery_service_is_injected_and_used` — custom DiscoveryService injection verified via mock

## Changed Files

| File | Change |
|------|--------|
| `backend/app/services/market_sync.py` | Import `DiscoveryService`; `MarketSyncService.__init__` accepts `discovery` param (default `DiscoveryService()`); `run()` uses `fetch_markets()` → `discovery.evaluate()` → `candidates` |
| `backend/tests/services/test_market_sync.py` | `_fetched()` default timestamps valid for discovery; `_mock_fetcher` mocks `fetch_markets`; `test_client_error_propagates` + `test_passes_limit_to_fetcher` updated; `TestSyncDiscoveryIntegration` class added |

## Discovery-Sync Entegrasyon Kararları

| Karar | Gerekçe |
|-------|---------|
| `fetch_markets()` → `discovery.evaluate()` akışı | `DiscoveryService` tek seçim otoritesi; `fetch_candidates()` artık sync yolunda değil |
| `fetch_candidates()` silinmedi | Hâlâ `POST /discover` için kullanılabilir; kaldırmak test kırar, scope dışı |
| `SyncResult` yapısı değişmedi | `fetched` = discovery candidates (pipeline girişi) — anlam korundu, API stabil |
| `discovery: DiscoveryService \| None = None` constructor param | Test edilebilirlik için injection; production'da default `DiscoveryService()` |
| `_fetched()` helper'a geçerli timestamp eklendi | Discovery kurallarını geçmesi için gerekli; mapper testleri etkilenmez |
| `bad` fixture'a geçerli timestamp eklendi | Discovery'den geçip mapper'da fail etmesi için — skipped_mapping semantiği doğru test edildi |

## Duplication Analizi

| Kural | Önce | Sonra |
|-------|------|-------|
| active/closed | `_is_5m_candidate` + `DiscoveryService` | Yalnızca `DiscoveryService` (sync yolunda) |
| duration [240-360]s | `_is_5m_candidate` + `DiscoveryService` | Yalnızca `DiscoveryService` (sync yolunda) |
| enableOrderBook, tokens | `_is_5m_candidate` | `_is_5m_candidate` (FetchedMarket'ta bu alanlar yok) |

`fetch_candidates()` `enableOrderBook`/`tokens` gate'i için hâlâ geçerli; ancak sync bu yoldan geçmiyor.
