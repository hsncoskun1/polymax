# Test Results — v0.5.5 Discovery Flow Integration Lock

**Date:** 2026-04-01
**Branch:** feat/v0.5.5-discovery-flow-integration
**Status:** PASSED

---

## Purpose

Unit tests verify each layer in isolation. Before this version, no test
verified that the layers actually work correctly **together** in the real
production path. v0.5.5 adds the first integration test layer.

**What is mocked:** Only `PolymarketClient.get_markets()` — the actual HTTP
network boundary.

**What runs real:** `PolymarketFetchService`, `DiscoveryService`,
`MarketSyncService`, FastAPI app (for endpoint tests).

---

## New Test File

`backend/tests/integration/test_discovery_flow.py`

---

## Architecture Contracts Locked

| Contract | Description | Tests |
|----------|-------------|-------|
| **C1** | `PolymarketFetchService` is a normaliser, not a selector. All records from the API must pass through `fetch_markets()` unchanged. | 2 |
| **C2** | `DiscoveryService` is the sole candidate-selection authority. All five rejection rules are applied only here. | 10 |
| **C3** | `MarketSyncService` respects `DiscoveryService` output. No extra filtering, no extra candidates. | 3 |
| **C4** | `/discover` endpoint surfaces only `DiscoveryService` candidates. HTTP response counts match discovery output. | 3 |

---

## Tests Added (18 total)

### Contract C1 — Fetcher is Normaliser (2 tests)

| Test | Why | Verifies |
|------|-----|----------|
| `test_fetch_returns_all_markets_including_invalid_ones` | Prevents fetcher from silently dropping invalid records | 5 invalid markets all survive `fetch_markets()` |
| `test_fetch_preserves_field_values_discovery_needs_to_evaluate` | Prevents field coercion hiding rejection signals | `active=False`, `enable_order_book=False`, `tokens=[]` survive normalisation intact |

### Contract C2 — Discovery is Sole Selector (10 tests)

| Test | Why | Verifies |
|------|-----|----------|
| `test_valid_5m_market_becomes_candidate` | Happy path | fetch returns it, discovery accepts it |
| `test_inactive_active_false_fetch_passes_discovery_rejects` | INACTIVE rule | fetch returns it, discovery assigns INACTIVE |
| `test_inactive_closed_true_fetch_passes_discovery_rejects` | INACTIVE rule (closed) | fetch returns it, discovery assigns INACTIVE |
| `test_no_order_book_false_fetch_passes_discovery_rejects` | NO_ORDER_BOOK rule | fetch returns it, discovery assigns NO_ORDER_BOOK |
| `test_no_order_book_absent_field_fetch_passes_discovery_rejects` | Conservative reject for absent field | None → NO_ORDER_BOOK via real normalisation |
| `test_empty_tokens_list_fetch_passes_discovery_rejects` | EMPTY_TOKENS rule | fetch returns it, discovery assigns EMPTY_TOKENS |
| `test_missing_start_date_fetch_passes_discovery_rejects` | MISSING_DATES rule | source_timestamp=None preserved, discovery rejects |
| `test_missing_end_date_fetch_passes_discovery_rejects` | MISSING_DATES rule | end_date=None preserved, discovery rejects |
| `test_duration_out_of_range_fetch_passes_discovery_rejects` | DURATION_OUT_OF_RANGE rule | 3600s duration rejected by discovery |
| `test_mixed_payload_only_valid_candidates_survive` | Full pipeline sanity | 6 markets → fetch returns all 6, discovery passes 1 |

### Contract C3 — Sync Respects Discovery (3 tests)

| Test | Why | Verifies |
|------|-----|----------|
| `test_sync_writes_only_discovery_candidates` | Prevents sync from writing rejected markets | Registry contains only valid candidates |
| `test_sync_all_invalid_payload_writes_nothing` | Edge case: no valid markets | Registry stays empty |
| `test_sync_valid_payload_produces_up_down_pairs_per_candidate` | Mapping correctness | Each candidate → UP + DOWN entry |

### Contract C4 — Endpoint Surfaces Discovery Output (3 tests)

| Test | Why | Verifies |
|------|-----|----------|
| `test_discover_endpoint_mixed_payload_returns_only_candidates` | Real fetcher+discovery, mock client | HTTP counts match discovery output |
| `test_discover_endpoint_all_valid_payload` | All-pass scenario | candidate_count=fetched_count |
| `test_discover_endpoint_all_invalid_payload` | All-reject scenario | candidate_count=0, breakdown keys present |

---

## Test Run

```
============================= 203 passed in 0.69s =============================
```

Previous count: 185 (v0.5.4)
Added: 18 integration tests
Net: 185 + 18 = 203 ✓

---

## Production Code Changes

**None.** No production code was modified in this version. The integration
tests were designed to fit the existing production architecture without
requiring any refactoring.

---

## Risk / Gaps

| Risk | Assessment |
|------|------------|
| PolymarketClient real HTTP calls | Not tested (expected — network tests belong in a separate E2E suite) |
| `MarketMapper` symbol extraction edge cases | Covered in unit tests; integration tests use BTC question so symbol resolves cleanly |
| Registry persistence | Not applicable (in-memory; persistence is a future milestone) |
| Concurrent sync / registry race conditions | Out of scope until a scheduler is added |
| `POST /sync` endpoint integration | Not covered at integration level; covered in `test_sync_api.py` at API level with mocked sync service |
