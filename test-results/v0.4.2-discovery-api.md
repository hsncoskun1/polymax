# Test Report — v0.4.2 Manual Discovery API

**Date:** 2026-04-01
**Status:** PASS

## Summary

| Suite | Tests | Passed | Failed |
|-------|-------|--------|--------|
| test_discover_api | 14 | 14 | 0 |
| Full backend suite | 175 | 175 | 0 |

## New Tests (test_discover_api.py)

- **Success scenarios** (5): 200 response, response shape, all valid → candidates, empty fetch, all rejected
- **Rejection breakdown** (5): all keys present, inactive counted, missing_dates counted, duration counted, fetched=candidate+rejected invariant
- **Error scenarios** (2): timeout → 504, upstream HTTP error → 502
- **Registry not touched** (2): successful discover writes nothing, error discover writes nothing

## New/Changed Files

| File | Change |
|------|--------|
| `backend/app/api/deps.py` | Added `get_discovery_service()` → `(PolymarketFetchService, DiscoveryService)` |
| `backend/app/api/markets.py` | Added `DiscoveryResponse` schema + `POST /discover` endpoint |
| `backend/tests/api/test_discover_api.py` | 14 new API tests |

## API/Discovery Entegrasyon Kararları

| Karar | Gerekçe |
|-------|---------|
| `fetch_markets()` kullanıldı, `fetch_candidates()` değil | Discovery'nin görevi zaten hangilerin geçip hangilerinin eleneceğini göstermek — ham listeyi `DiscoveryService` değerlendirmeli |
| Dependency `tuple[Fetcher, Discovery]` döndürüyor | İki bağımsız servis, tek dependency fonksiyondan geliyor — basit, test edilebilir |
| `rejection_breakdown` string key olarak döndü (`r.value`) | JSON'da enum objesi değil, sade string key (`"inactive"` vb.) daha uyumlu |
| Candidate listesi response'a dahil edilmedi | Payload şişmesi riski; sayısal özet amacı karşılar; gerekirse ileride eklenir |
| Registry dokunulmadı — bağımlılık bile yok | Endpoint tamamen read-only; `get_registry` dependency'si yok |

## Risk/Notlar

- v0.4.1 main'e merge edilmemişti; `cherry-pick` ile bu branch'e alındı — iki PR birlikte merge edilebilir ya da v0.4.2 üstüne squash yapılabilir
- `POST /discover` ve `POST /sync` farklı akışlar: discover yazar değil, sync yazar
