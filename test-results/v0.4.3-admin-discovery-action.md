# Test Report — v0.4.3 Admin Discovery Action

**Date:** 2026-04-01
**Status:** PASS

## Summary

| Suite | Tests | Passed | Failed |
|-------|-------|--------|--------|
| Frontend (App.test.tsx) | 21 | 21 | 0 |
| Backend (unchanged) | 175 | 175 | 0 |

## New Frontend Tests (DiscoverAction suite, App.test.tsx)

- `renders Discover now button on admin panel`
- `shows loading state while discovery is in progress`
- `renders discovery summary on successful response`
- `renders rejection breakdown on successful response`
- `shows error message when discover request fails`

## New/Changed Files

| File | Change |
|------|--------|
| `frontend/src/lib/api.ts` | Added `DiscoverResult` interface + `triggerDiscover()` |
| `frontend/src/components/DiscoverAction.tsx` | New — Discover now button, loading/error/success states, DiscoverSummary with breakdown |
| `frontend/src/pages/AdminPanel.tsx` | Added `<DiscoverAction />` above `<SyncAction />` |
| `frontend/src/tests/App.test.tsx` | Added DiscoverAction import + 5-test suite |

## UI/Data Binding Kararları

| Karar | Gerekçe |
|-------|---------|
| SyncAction pattern birebir takip edildi | Tutarlı UI/UX; `idle → loading → success/error` union state |
| `DiscoverAction` onSuccess prop almıyor | Discovery registry'ye yazmıyor; market listesi değişmez, refresh gerekmez |
| `rejection_breakdown` label map'i (BREAKDOWN_LABELS) | API key'leri (`inactive`, `missing_dates`) doğrudan kullanıcıya gösterilmez; okunabilir label'a çevrildi |
| Breakdown section sadece entry varsa render edilir | Boş breakdown durumu sessizce atlanır |
| `fetch_markets()` kullanıldığı için payload büyük olabilir | Discovery card sayısal özet gösteriyor, raw liste göstermiyor — payload UI'a yansımıyor |

## Notlar

- `act()` uyarıları önceki testlerde de mevcut; async state güncellemelerinden kaynaklanıyor, test başarısız değil
- Backend değiştirilmedi
