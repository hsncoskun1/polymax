# v0.5.24 — Repo Cleanup and Consistency Pass

**Date:** 2026-04-01
**Branch:** (direct to main — pre-branch-discipline)
**Status:** Complete

---

## Scope

Frontend/backend interface alignment and single-source-of-truth enforcement for
host/port configuration. No new features, no new contracts.

---

## Changes made

### Files modified

| File | Change |
|------|--------|
| `frontend/src/lib/api.ts` | Added missing `end_date: string \| null` to `Market` interface; added missing `registry_total_count: number` to `SyncResult` interface |
| `frontend/src/components/SyncAction.tsx` | Added "Registry total" row to `SyncSummary` display |
| `frontend/src/tests/App.test.tsx` | Updated `MOCK_MARKETS` to include `end_date: null`; updated `MOCK_SYNC_RESULT` to include `registry_total_count: 4` |
| `launcher/main.py` | Replaced hardcoded host/port defaults with `_load_launcher_config()` reading `config/default.toml` via `tomli` |

### Removed

- 5 Windows path artefact files/dirs at repo root (e.g. `C:CLAUDECODEbackendtestsfixtures`)
  created by earlier git operations on Windows. Removed with `rm -rf`.

---

## Test results

- **Tier:** Standard
- **Result:** All automated tests passed (count at milestone: 594 passed, 1 skipped)
- **Scope:** No new tests added in this milestone

---

## Remaining risks

- `frontend/src/lib/api.ts` interfaces are maintained manually — no automated
  contract enforcement between backend Pydantic models and frontend TypeScript types.
  Any future backend field addition requires a parallel frontend update.
