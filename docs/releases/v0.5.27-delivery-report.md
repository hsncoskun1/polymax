# v0.5.27 — UI ↔ Backend Wiring Audit

**Date:** 2026-04-01
**Branch:** `fix/ui-backend-wiring-gaps`
**Status:** Complete

---

## Scope

Audit every user-facing UI surface for genuine backend wiring. Identify
and close silent no-op, false control, or display-only state that does not
reflect real backend behaviour.

---

## Component → API → Backend map

| UI surface | Component | API call | Backend handler | Wired |
|-----------|-----------|----------|-----------------|-------|
| Health badge | `HealthBadge` | `GET /health` (30 s poll) | `health_check()` | ✅ |
| Market list | `MarketList` | `GET /api/v1/markets` | `list_markets()` | ✅ |
| Sync button | `SyncAction` | `POST /api/v1/markets/sync` | `trigger_sync()` | ✅ |
| Sync → market refresh | `App.tsx` refreshKey chain | — | — | ✅ |
| Discover button | `DiscoverAction` | `POST /api/v1/markets/discover` | `trigger_discover()` | ✅ |
| Admin version chips | `AdminPanel` StatusChip | none | none | ❌ → fixed |

---

## Findings and fixes

| # | Finding | Risk | Fix |
|---|---------|------|-----|
| 1 | `SyncSummary` didn't render `rejected_count` / `rejection_breakdown` (in backend since v0.5.9, typed but not displayed) | MEDIUM NON-BLOCKER | Added "Rejected" row and breakdown section to `SyncSummary` |
| 2 | `DiscoverAction` BREAKDOWN_LABELS missing `no_order_book` and `empty_tokens` | LOW NON-BLOCKER | Two labels added |
| 3 | `AdminPanel` Backend/Frontend chips showed hardcoded `"v0.1.0"` | MEDIUM NON-BLOCKER | Changed to `"—"` (honest placeholder) |

---

## Changes made

| File | Change |
|------|--------|
| `frontend/src/components/SyncAction.tsx` | BREAKDOWN_LABELS added; SyncSummary renders rejected_count + breakdown |
| `frontend/src/components/DiscoverAction.tsx` | Added `no_order_book` and `empty_tokens` labels |
| `frontend/src/pages/AdminPanel.tsx` | Hardcoded `"v0.1.0"` → `"—"` |
| `frontend/src/tests/App.test.tsx` | MOCK_SYNC_RESULT updated with `rejected_count: 0`, `rejection_breakdown: {}` |

---

## Test results

- Backend: 594 passed, 1 skipped ✅
- Frontend: 21 passed ✅

---

## Remaining risks

- `AdminPanel` Backend chip now shows `"—"` — honest but not informative.
  Wiring to real backend version is follow-up work (v0.5.28).
- Frontend version chip has no authoritative source in this repo.
